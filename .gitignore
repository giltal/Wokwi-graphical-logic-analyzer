# Build artifacts
/build/
/chip/dist/*
# ...except the amalgamated single-file source, which wokwi.com users copy
# straight out of the repo (regenerated by every build and by CI).
!/chip/dist/logic-scope.chip.c
sdkconfig
sdkconfig.old
/managed_components/
/dependencies.lock

# Wokwi
wokwi.vcd

# Editor
.vscode/settings.json

# Local-only helper scripts
*.local.ps1
